[push]
	default = simple
[user]
	email = me388@cam.ac.uk
	name = Matthew Evans
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
