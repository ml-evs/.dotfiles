# bemenu defaults to its X11 backend; under sway we want wayland-native.
# Read by user-session systemd, propagated to dbus-activated services
# (including xdg-desktop-portal-wlr, which uses bemenu as its chooser).
BEMENU_BACKEND=wayland
